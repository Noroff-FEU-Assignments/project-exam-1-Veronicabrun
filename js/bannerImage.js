// Henter bilde fra Wordpress REST API
//fetch( 'https://veronicabp.com/')
  //.then(response => response.json())
  //.then(data => {
    //const imageURL = data.source_url; // Anta at 'source_url' er URL-en til bildet
    //applyBackgroundImage(imageUrl); // Kall funksjonen for å bruke bildet som bakgrunnsbilde
  //})

  //.catch(error => console.error('Error fetching image:', error));

  // Funskjon for å bruke bildet som bakgrunnsbilde
  //function applyBackgroundImage(imageUrl) {
    //const bannerContainer = document.querySelector('.banner-container');
    //bannerContainer.style.backgroundImage = 'url(${imageUrl})';
  //}